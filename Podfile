# Podfile

platform :ios, '13.0'
use_frameworks!
inhibit_all_warnings!

target 'JMarvel' do
  
  pod 'SwiftLint'
  pod 'Kingfisher'
  
  target 'JMarvelTests' do
    inherit! :search_paths
  end

end
